import { ShortarrayPipe } from './shortarray.pipe';

describe('ShortarrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortarrayPipe();
    expect(pipe).toBeTruthy();
  });
});
